name: My First CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello JumpToTech CI"

      - name: Test Pass
        run: exit 0

       echo "CI PASSED"
